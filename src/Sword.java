class Sword {
    public double baseDamage;

    public Sword(double baseDamage) {
        this.baseDamage = baseDamage;
    }
    // baseDamage will get when it called and prepare to be calculated in next step
    
    public double getBaseDamage() {
        return baseDamage;
    }
    // Can be called to check get its base damage
}